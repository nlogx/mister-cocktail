<%= simple_form_for @cocktail do |f| %>
<%= f.input :name %>

<!-- add image -->
<%= f.input :photo %>
<%= f.input :photo_cache, as: :hidden %>

<%= f.button :button, type: :submit do %>
<%= icon('fas', 'check') %>
<% end %>
<% end %>
